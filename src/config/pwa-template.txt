<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
    <title>Light & Night: Dreamline</title>
    
    <!-- PWA 移动端适配 Meta -->
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <meta name="theme-color" content="#f9f9f9">
    <link rel="apple-touch-icon" href="assets/app-icon.png">
    
    <!-- 引用 Manifest (指向下方嵌入的虚拟文件) -->
    <link rel="manifest" href="manifest.json">

    <!-- ⚠️ 请在这里保留你原有的 CSS 和 外部库引用 (如 font-awesome 等) -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style>
        /* [⚠️ 粘贴区] 
           请在这里粘贴你原有的 <style> 标签内的全部 CSS 代码 
        */
        :root { --brand-pink: #ff96b8; }
        body { margin: 0; font-family: sans-serif; background: #f9f9f9; }
    </style>
</head>
<body>

    <!-- 
       [⚠️ 粘贴区] 
       请在这里粘贴你原本 <body> 标签内的所有 HTML 元素 (如 <div id="app">, <header> 等) 
    -->
    <div id="app">
        <!-- 原有结构占位 -->
    </div>

    <!-- 
       [⚠️ 粘贴区] 
       请在这里粘贴你原本脚本中的业务逻辑、State 变量、函数等 
    -->
    <script>
        // 原有业务逻辑代码粘贴于此
        console.log("Original logic loaded.");
    </script>


    <!-- ==========================================
         PWA 核心文件嵌入区 (请勿修改此处 script 标签格式)
         ========================================== -->

    <!-- 嵌入 Manifest 文件 -->
    <script type="text/file" data-filename="manifest.json">
    {
      "name": "Light & Night: Dreamline",
      "short_name": "Dreamline",
      "description": "光与夜之恋：织梦网页版",
      "start_url": "./index.html",
      "display": "standalone",
      "background_color": "#f9f9f9",
      "theme_color": "#ff96b8",
      "icons": [
        {
          "src": "assets/app-icon.png",
          "sizes": "192x192",
          "type": "image/png"
        },
        {
          "src": "assets/app-icon.png",
          "sizes": "512x512",
          "type": "image/png"
        }
      ]
    }
    </script>

    <!-- 嵌入 Service Worker 文件 -->
    <script type="text/file" data-filename="sw.js">
    const CACHE_NAME = 'light-night-dreamline-v1';
    const ASSETS_TO_CACHE = [
      './index.html',
      'https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css'
    ];

    // 安装阶段：缓存核心资源
    self.addEventListener('install', (event) => {
      event.waitUntil(
        caches.open(CACHE_NAME).then((cache) => {
          return cache.addAll(ASSETS_TO_CACHE);
        })
      );
    });

    // 激活阶段：清理旧缓存
    self.addEventListener('activate', (event) => {
      event.waitUntil(
        caches.keys().then((cacheNames) => {
          return Promise.all(
            cacheNames.map((name) => {
              if (name !== CACHE_NAME) return caches.delete(name);
            })
          );
        })
      );
    });

    // 拦截请求：优先从缓存读取
    self.addEventListener('fetch', (event) => {
      event.respondWith(
        caches.match(event.request).then((response) => {
          return response || fetch(event.request);
        })
      );
    });
    </script>

    <!-- PWA 自动注册逻辑 -->
    <script>
        /**
         * 注意：虽然我们将代码嵌入在 script 中，
         * 但在静态网页环境下，浏览器通常需要一个物理存在的 sw.js 文件来完成 Service Worker 的注册。
         * 如果你在支持生成文件的托管平台（如某些特定的 Web IDE）上运行，以下代码将尝试寻找物理文件。
         */
        if ('serviceWorker' in navigator) {
            window.addEventListener('load', () => {
                navigator.serviceWorker.register('./sw.js')
                    .then(reg => console.log('PWA Service Worker 注册成功:', reg.scope))
                    .catch(err => console.log('PWA Service Worker 注册失败:', err));
            });
        }
    </script>

</body>
</html>